version: '2'
services:
  linguistics-mongodb:
    image: mongo:4.2.6
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/linguistics-mongodb-config --bind_ip 0.0.0.0
  linguistics-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  linguistics-mongodb-config:
    image: mongo:4.2.6
    container_name: linguistics-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
